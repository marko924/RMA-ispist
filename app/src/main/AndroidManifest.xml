<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/app_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DriveNow"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        tools:targetApi="31">
        <activity android:name=".views.LoginActivity"
            android:exported="false"/>
        <activity android:name=".views.RegistrationActivity"
            android:exported="false"/>
        <activity android:name=".views.KorisnikActivity"
            android:exported="false"/>
        <activity android:name=".views.AutomobiliActivity"
            android:exported="false"/>
        <activity android:name=".views.BookingActivity"
            android:exported="false"/>
        <activity android:name=".views.BookingDetailsActivity"
            android:exported="false"/>
        <activity android:name=".views.BookingProcesActivity"
            android:exported="false"/>
        <activity android:name=".views.DetailsActivity"
            android:exported="false"/>
        <activity android:name=".views.ProfileActivity"
            android:exported="false"/>
        <activity android:name=".views.ProfileIzmeniActivity"
            android:exported="false"/>
        <activity
            android:name=".views.MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>